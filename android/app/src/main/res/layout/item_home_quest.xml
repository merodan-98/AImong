<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="12dp"
    android:background="@drawable/bg_quest_item"
    android:padding="16dp">

    <!-- 이모지 타일 박스 -->
    <TextView
        android:id="@+id/tv_quest_emoji"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:background="@drawable/bg_emoji_tile"
        android:gravity="center"
        android:text="🎒"
        android:textSize="24sp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="16dp"
        android:layout_marginEnd="12dp"
        android:orientation="vertical"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toStartOf="@+id/btn_action_container"
        app:layout_constraintStart_toEndOf="@+id/tv_quest_emoji"
        app:layout_constraintTop_toTopOf="parent">

        <TextView
            android:id="@+id/tv_quest_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="AI 퀴즈 3문제 맞히기"
            android:textColor="@color/text_white"
            android:textSize="15sp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/tv_quest_reward"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:text="+50 EXP · 가챠 티켓 1장"
            android:textColor="@color/text_sub_grey"
            android:textSize="12sp" />
    </LinearLayout>

    <!-- 액션 영역 (그라데이션 버튼 또는 체크표시) -->
    <FrameLayout
        android:id="@+id/btn_action_container"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent">

        <ImageView
            android:id="@+id/iv_completed"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:src="@drawable/bg_quest_completed_check"
            android:visibility="gone" />

        <TextView
            android:id="@+id/tv_start_btn"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="@drawable/bg_button_start_gradient"
            android:drawableStart="@drawable/ic_play_arrow_white"
            android:drawablePadding="4dp"
            android:paddingHorizontal="12dp"
            android:paddingVertical="8dp"
            android:text="시작"
            android:textColor="@color/text_white"
            android:textSize="13sp"
            android:textStyle="bold" />
    </FrameLayout>

</androidx.constraintlayout.widget.ConstraintLayout>
